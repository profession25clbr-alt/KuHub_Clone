primero tiene que se preparar al consulta donde al seleccionar la semana segun el selecctor indicado anteriormente que es semejante a lo que esta en pedido semanal bodega para el filtrado, tiene que aparecer al precionar el btn generar order compra
al seleccionar el btn tendra que usar un consulta que mueste el seguinte
los pedidos existentes para esta semana encuentrado puede ser mas de uno.
mostrar si existe una orden de compra generada anteriormente a cierto pedido consultado, si la subcosulta se encuentra mas de una orden de compra dado que la tabla orden de compra tiene la id_pedido mostraremos un mensaje al lado 'Ya existe un registro para este pedido' no impedindo que seleccione el pedido anteriormente registrado.
tiene que mostrar solamente los pedidos con el estado APROBADO

al cargar n pedidos el usuario seleccionara los pedidos que quiere crear la orden de compra, que servira para buscar todos los detalles de los pedidos para tener los ids de productos con las cantidades correspondientes.
se mostrara la tabla con el nombre_producto, unidad_medida el valor abreviado, la cantidad solicitada que es la cantidad total sumada del mismo producto de entre todos los pedidos

adado a esto los ids del pedido es lo que ayudara a realizar el filtrado correspondiente.