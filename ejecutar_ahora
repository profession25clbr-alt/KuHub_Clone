en la page Administacion de sistema, hay una vista que se llama Gestion del Sistema donde actualmente hay un checkbox donde dice que los pedidos se aceptan automaticamente si tiene la solicitud con el estado ACEPTADA, esta funcional y se acepta las solicitudes vinculando a un pedido existe o creando un pedido este funcionamiento ya esta funcional, lo que necesito hacer es que si esta marcado o mejor diciente el valor en la bbdd de esta tabla es true que es marcado, entonce hay que ocultar la page de Gestion de Pedidos que sieve para aceptar manualmente de manera mas controlada en casos de multiples roles de responsabilidad. la tabla se llama
-- Tabla configuraciones del sistema
CREATE TABLE IF NOT EXISTS gestion_sistema (
  id                      SERIAL PRIMARY KEY,
  solicitudes_en_pedido   BOOLEAN NOT NULL DEFAULT FALSE,
  descripcion             VARCHAR(255)
);
los valores actuales son 2 inserts uno para restabelecer al predeterminado y otro para la modificacion constante.
-- Fila 1: configuración default (solo lectura, para restaurar)
INSERT INTO gestion_sistema (id, solicitudes_en_pedido, descripcion)
VALUES (1, FALSE, 'Configuración predeterminada del sistema - NO MODIFICAR')
ON CONFLICT (id) DO NOTHING;

-- Fila 2: configuración activa (la que el panel modifica)
INSERT INTO gestion_sistema (id, solicitudes_en_pedido, descripcion)
VALUES (2, FALSE, 'Configuración activa del sistema')
ON CONFLICT (id) DO NOTHING;
